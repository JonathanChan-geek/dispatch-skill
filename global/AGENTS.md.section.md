<!-- 可选片段。只合并进当前 canonical AGENTS.md；安装器不会自动修改全局规则。 -->

## Astra 规划，Kimi 执行

GPT Astra 在当前主控会话中负责规划、拆解、关键决策和验收。Kimi Code 是唯一执行者，负责快速完成简单、明确、已经设计好的具体步骤。复杂问题先由 Astra 想清楚再拆小，不新建 GPT/Codex worker，也不默认安排跨模型评审。

使用 `dispatch` skill 将具体执行单交给 Kimi。出现歧义或设计问题，Kimi 返回阻塞点，由 Astra 决策并补充下一步。真正独立且有收益的任务才并行，连续决策保持串行。项目事实以 canonical `AGENTS.md` 为准；`CLAUDE.md` 如需保留，只作兼容入口。

worker 的报告只是待验收材料。主控核对修改范围、实际 diff 和原始验证输出后给结论。测试规模与改动风险相称；没有新改动、新失败或未解决疑点时，不重复扩大测试。
