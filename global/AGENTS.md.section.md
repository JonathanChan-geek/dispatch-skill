<!-- 可选片段。只合并进当前 canonical AGENTS.md；安装器不会自动修改全局规则。 -->

## 多模型派单

当前 GPT 是主控，负责架构、拆单、连续决策、监工和验收。只有独立任务值得委派时才加载 `dispatch` skill：后端/复杂逻辑给 Codex worker，文档/机械改动/测试补写给 Kimi，前端/UI 和紧密耦合工作由主控完成。

项目路由写在项目 canonical `AGENTS.md` 的“派单路由”节。`CLAUDE.md` 如需保留，只作为兼容入口，不复制规则正文。

worker 的报告只是待验收材料。主控核对修改范围、实际 diff 和原始验证输出后给结论。测试规模与改动风险相称；没有新改动、新失败或未解决疑点时，不重复扩大测试。
