{
  "name": "gpt-dispatch-skill",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "GPT Astra plans and reviews; Kimi Code executes concrete tasks",
  "engines": { "node": ">=20" },
  "scripts": { "test": "node --test tests/*.test.mjs", "check": "node scripts/check.mjs" }
}
